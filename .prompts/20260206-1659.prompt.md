In @src/SharpClawCli/Program.cs implement a new command line argument --yolo -y 

If that argument is provided, the application should still print tool invocations, but it should not Console.ReadLine() for user input